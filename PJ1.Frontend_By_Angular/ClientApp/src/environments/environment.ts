// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

//Этот файл может быть заменен во время сборки с помощью массива
// `fileReplacements`.
// `ng build` Заменяет `environment.ts` с `environment.prod.ts`.
// Список замены файлов можно найти в `angular.json`.

export const environment = {
    production: false
};

/*
 * For easier debugging in development mode, you can import
 *  the following file
 * to ignore zone related error stack frames such
 *  as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode
 *  because it will have a negative impact
 * on performance if an error is thrown.
 * 
 * Для облегчения отладки в режиме разработки вы можете импортировать
 *  следующий файл
 * Чтобы игнорировать зону, связанные с ошибками стека кадров,
 *  таких как `ZONE.RUN`,` zonedelegate.invoketask`.
 *
 * Этот импорт должен быть прокомментирован в производственном 
 * режиме, потому что он окажет негативное воздействие
 * О производительности, если ошибка брошена.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
